# DYL-35 — Account-owned Night Ritual playmat
Night Ritual must leave the global surface list and static public asset path. Add an account-owned playmat library displayed as My playmats in the picker; retain the generic built-in surfaces. Store owner-scoped asset records and private image files under the existing configured asset directory. Reuse this storage for newly uploaded custom playmats so a user can select their own saved mats across decks. Keep existing custom deck uploads functional.
The coordinator will seed the existing Night Ritual image only for the explicitly identified owner account in each environment, through a local operator helper. Do NOT hardcode email, grant all admins access, auto-grant first user, or ask Cursor to access account data. Provide an idempotent helper accepting explicit owner ID/image path. No production operations in this session.
Enforce ownership server-side for listing, image retrieval and selecting saved assets; forged IDs or legacy night-ritual commands must not grant access. Other users and shared/public deck projections must not expose another account's private mat or asset path; default surface on unauthorized/shared views. Move the existing bundled jpg out of public static distribution (coordinator retains original for seeding); remove CSS global references. Existing non-owner night-ritual presentations should safely fall back, while the owner's selection can be migrated by the operator helper. No deletion of accounts/decks or unrelated images. Ensure copies, undo/history, upload replacement and deck deletion cannot cross ownership or delete reusable library images. Add meaningful two-user route/repository tests, migration/legacy handling and picker behavior. Additive schema only.
Own playmat code only; another session owns text import and a third owns Research loading UI. Prefer a separate account_playmats.py module and narrowly scoped edits to shared files.

Coordinator review clarification: Authenticated private playmat image responses must use private/no-store cache policy, including deck-image and direct-library routes. Migration occurs at application schema setup, not on every image/list GET.
