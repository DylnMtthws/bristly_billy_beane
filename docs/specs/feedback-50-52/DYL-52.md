# DYL-52 — Commander-legal, identity-scoped search
Research Cards results must be affirmative Commander legal, across every filter/query/pagination path; banned, not_legal and unknown excluded. Current format_legal_sql is affirmative already: locate actual bypasses, inconsistent cached/provider fallback or imported catalog flags rather than merely duplicating a condition. Add regression tests that exercise rendered Research results and count/pagination.
Builder Playmat and Decklist share /api/cards and must constrain results to subset of combined current commander/partner color identities; colorless valid, colorless commander means only colorless, missing commander leaves legal results unscoped. Derive identity server-side from owned current deck, never client-provided colors. Unowned/missing decks fail closed. Validate same behavior in both views and after commander changes. Existing cards stay in deck and get DYL-51 warnings instead of deletion.
If baseline already filters correctly, demonstrate it and investigate client stale results or alternate path/data source. New regressions must prove actual API results against mixed legalities and identities. Coordinate with DYL-50 for efficient owned-deck lookup. Preserve commander picker behavior and legal partner choices. No hardcoded card bans or production data edits.
