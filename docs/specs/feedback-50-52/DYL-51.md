# DYL-51 — Deck count and actionable legality indicators
Show a clear X/100 count in the decklist (also visible in playmat shared toolbar), including commander(s); red below or above 100. Exactly 100 uses normal valid styling, regardless of separate card issues. Main-deck zones count; existing sideboard/maybeboard exclusion stays intact.
Return structured per-entry validation reasons from backend and show affected cards red in list/grid/spoiler and playmat, with accessible text/icon/title explaining each reason. Preserve overall validation issues and existing callers. Update live after every mutation/undo/redo/commander change, including read-only public deck views. Never delete cards or block editing due to warnings.
Rules: aggregate copies across entries/printings and zones by oracle identity (name fallback), including a commander duplicate in library. Basic lands exempt. Oracle text allowing any number exempt; finite 'up to N cards named' limits must be enforced (Nazgûl nine, Seven Dwarves seven). Don't treat finite limits as unlimited. Honor basic snow lands. Test split entries and distinct printings, below/at/above limit. Color identity must be subset of combined commander identities; colorless allowed; missing commander is a deck-level warning, not every card red. Flag affirmative nonlegal/unknown catalog legality conservatively for loaded cards; existing saved/imported illegal cards remain available for correction. Flag illegal commanders as well.
Separate deck count status from per-card legality. Don't conflate mana cost/color with color identity. No hardcoded ban list. Prefer oracle-text limits with known finite numeral parsing and tests rather than name-only exceptions. Use concise styling consistent with existing site. Add tests for total 99/100/101, valid pairs, excluded sideboard, duplicates/basic/unlimited/finite, identity unions, format illegal entries, live rendering hooks. Search implementation belongs to other task.
